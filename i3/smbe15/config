# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Style
new_window 1pixel
#set border none

set $mod Mod4

set $key_a 38
set $key_s 39
set $key_d 40
set $key_f 41

set $key_g 42
set $key_h 43

set $key_j 44
set $key_k 45
set $key_l 46
set $key_ö 47

set $key_y 52
set $key_x 53
set $key_c 54
set $key_v 55
set $key_b 56

set $key_q 24
set $key_w 25
set $key_e 26
set $key_r 27

set $key_t 28
set $key_z 29

set $key_u 30
set $key_i 31
set $key_o 32
set $key_p 33

set $key_n 57
set $key_m 58
set $key_comma 59
set $key_dot 60
set $key_minus 61

set $key_quote 20
set $key_roof 21

set $key_mute 121
set $key_voldown 122
set $key_volup 123

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# font 57-dejavu-sans

# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec gnome-terminal

# kill focused window
bindcode $mod+Shift+$key_x kill

# start dmenu (a program launcher)
bindcode $mod+$key_d exec ~/.config/i3/dmenui
#bindcode $mod+$key_d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindcode $mod+$key_j focus left
bindcode $mod+$key_k focus down
bindcode $mod+$key_l focus up
bindcode $mod+$key_ö focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindcode $mod+Shift+$key_j move left
bindcode $mod+Shift+$key_k move down
bindcode $mod+Shift+$key_l move up
bindcode $mod+Shift+$key_ö move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindcode $mod+$key_h split h

# split in vertical orientation
bindcode $mod+$key_g split v

# enter fullscreen mode for the focused container
bindcode $mod+$key_f fullscreen
#Fullscreen over all screens
bindcode $mod+Shift+$key_f fullscreen global
# change container layout (stacked, tabbed, toggle split)
bindcode $mod+$key_c layout stacking
bindcode $mod+$key_v layout tabbed
bindcode $mod+$key_b layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindcode $mod+$key_a focus parent
# focus the child container
bindcode $mod+Shift+$key_a focus child

#Put windows on a invisible Stack
bindcode $mod+Shift+$key_minus move scratchpad
bindcode $mod+$key_minus scratchpad show

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

bindsym $mod+Ctrl+1 workspace c1
bindsym $mod+Ctrl+2 workspace c2
bindsym $mod+Ctrl+3 workspace c3
bindsym $mod+Ctrl+4 workspace c4
bindsym $mod+Ctrl+5 workspace c5
bindsym $mod+Ctrl+6 workspace c6
bindsym $mod+Ctrl+7 workspace c7
bindsym $mod+Ctrl+8 workspace c8
bindsym $mod+Ctrl+9 workspace c9
bindsym $mod+Ctrl+0 workspace c10

bindcode $mod+$key_y exec ~/.config/i3/get_workspace_options.py | dmenu -b | ~/.config/i3/go_to_workspace.py
bindcode $mod+Ctrl+$key_y exec dmenu -b | ~/.config/i3/go_to_workspace.py
bindcode $mod+Shift+$key_y exec ~/.config/i3/get_workspace_options.py | dmenu -b | ~/.config/i3/move_to_workspace.py
bindcode $mod+Ctrl+Shift+$key_y exec dmenu -b | ~/.config/i3/move_to_workspace.py

#goto new workspace
bindcode $mod+Shift+$key_v exec "date|md5sum|cut -c-5|~/.config/i3/go_to_workspace.py"

bindcode $mod+$key_quote workspace prev
bindcode $mod+$key_roof workspace next

bindsym $mod+Tab workspace back_and_forth
bindsym $mod+Shift+Tab move container to workspace back_and_forth


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# move focused container to workspace
bindsym $mod+Ctrl+Shift+1 move container to workspace c1
bindsym $mod+Ctrl+Shift+2 move container to workspace c2
bindsym $mod+Ctrl+Shift+3 move container to workspace c3
bindsym $mod+Ctrl+Shift+4 move container to workspace c4
bindsym $mod+Ctrl+Shift+5 move container to workspace c5
bindsym $mod+Ctrl+Shift+6 move container to workspace c6
bindsym $mod+Ctrl+Shift+7 move container to workspace c7
bindsym $mod+Ctrl+Shift+8 move container to workspace c8
bindsym $mod+Ctrl+Shift+9 move container to workspace c9
bindsym $mod+Ctrl+Shift+0 move container to workspace c10

# move workspace between outputs
bindcode $mod+Shift+$key_quote move workspace to output left
bindcode $mod+Shift+$key_roof move workspace to output up

# reload the configuration file
bindcode $mod+Shift+$key_n reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindcode $mod+Shift+$key_m restart
bindcode $mod+$key_m exec synclient TouchpadOff=$((1-`synclient|grep TouchpadOff|sed  -e 's/[^0-9]*//'`))
# exit i3 (logs you out of your X session)
bindcode $mod+$key_comma exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindcode $mod+Ctrl+$key_comma exec "i3-nagbar -t warning -m 'Shutdown?' -b 'Yes' 'shutdown now'"
bindcode $mod+Ctrl+Shift+$key_comma exec "setxkbmap ch; i3-msg workspace @lock; i3lock -c FF9900 -d;sudo pm-hibernate"
bindcode $mod+Shift+$key_comma exec "setxkbmap ch; gnome-session-quit"

bindcode $mod+Shift+$key_dot exec "setxkbmap ch; i3-msg workspace @lock; i3lock -c FF9900 -d"
bindcode $mod+$key_dot exec "setxkbmap ch; i3-msg workspace @lock; i3lock -i ~/Downloads/win7login1.png -d -u -p win"

bindcode $mod+Ctrl+$key_m exec "i3-msg workspace @matrix; xterm -e cmatrix;"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindcode $key_j resize shrink width 10 px or 10 ppt
        bindcode $key_k resize grow height 10 px or 10 ppt
        bindcode $key_l resize shrink height 10 px or 10 ppt
	bindcode $key_ö resize grow width 10 px or 10 ppt
	
        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
	bindcode $mod+$key_s mode "default"
	bindcode $key_s mode "default"
}

bindcode $mod+$key_s mode "resize"

mode "command" {
        bindsym Return mode "default"
        bindsym Escape mode "default"
	bindcode $mod+$key_s mode "resize"
	bindcode $mod+Shift+$key_s mode "resize"
	bindcode $key_s mode "default"
}

bindcode $mod+Shift+$key_s mode "command"
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	# mode hide
        status_command i3status
}

# autostart
exec --no-startup-id nm-applet

#ch the only true keyboard layout
exec "setxkbmap ch"
#For those N00bs which use an obsolete layout
bindcode $mod+Shift+Control+$key_c exec setxkbmap ch
bindcode $mod+Shift+Control+$key_u exec setxkbmap us
bindcode $mod+Shift+Control+$key_d exec setxkbmap dvorak
bindcode $mod+Shift+Control+$key_n exec setxkbmap de neo

bindcode $mod+$key_q exec qutebrowser
bindcode $mod+$key_w exec thunderbird
bindcode $mod+$key_e exec emacs
bindcode $mod+$key_r exec chromium
bindcode $mod+Shift+$key_r exec chromium --incognito
bindcode $mod+$key_t exec tuxcmd
bindcode $mod+$key_z exec nautilus --no-desktop
# move workspace between outputs

# open certain windows in floating mode
# xprop tool can be used to get WM_CLASS, WM_NAME properties
# for_window [class="(?i)gcalctool"] floating enable
# for_window [title="Dateioperationen"] floating enable
for_window [title="Auto-Complete"] floating enable
for_window [title="ec"] foating enable

# Audio
bindcode $key_mute exec "pulseaudio-ctl mute"
bindcode $key_voldown exec "pulseaudio-ctl down"
bindcode $key_volup exec "pulseaudio-ctl up"

##Auto assign new windows to workspaces
assign [class="(?i)mumble"] mumble

workspace 1 output DVI-D-1
workspace 2 output DVI-D-1
workspace 3 output DVI-D-1
workspace 4 output DVI-D-1
workspace 5 output DVI-D-1
workspace 6 output DVI-D-1
workspace 7 output DVI-D-1
workspace 8 output DVI-D-1
workspace 9 output DVI-D-1
workspace 10 output DVI-D-1
workspace c1 output DVI-D-0

exec --no-startup-id dbus-send \
    --session \
    --print-reply=literal \
    --dest=org.gnome.SessionManager \
    "/org/gnome/SessionManager" \
    org.gnome.SessionManager.RegisterClient \
    "string:i3" \
    "string:$DESKTOP_AUTOSTART_ID"
